spring:
  profiles.active: local
  application:
     name: quotes-service
security:
  basic:
    enabled: false
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}
